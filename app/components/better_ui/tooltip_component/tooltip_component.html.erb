<div class="relative inline-flex group <%= @container_classes %>" <%= tag.attributes(@options) %>>
  <%= content %>
  <div class="<%= tooltip_classes %>" role="tooltip">
    <%= @text %>
  </div>
</div>
